name: Java CI integrationTests and Build Docker image

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  build:

    runs-on: ${{matrix.os}}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
        java: [17]

    services:
      mysql:
        image: mysql:8
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: customerdb
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping -h localhost -uroot -proot"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=10

    steps:
    - uses: actions/checkout@v4
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: ${{matrix.java}}
        distribution: 'temurin'
        cache: maven


    - name: Build with Maven
      run: mvn test

    - name: Build with Maven
      run: mvn -B package -DskipTests

    - name: Login to Dockerhub
      run: echo ${{secrets.DOCKERHUB_TOKEN}} | docker login -u ${{secrets.DOCKERHUB_USERNAME}} --password-stdin

    - name: build image
      run: |
        docker build -t ${{secrets.DOCKERHUB_USERNAME}}/customer-service-java25:${{github.run_number}} .

    - name: push to docker hub
      run: |
        docker push ${{secrets.DOCKERHUB_USERNAME}}/customer-service-java25:${{github.run_number}}


      env:
        SPRING_DATASOURCE_URL: jdbc:mysql://localhost:3306/customerdb
        SPRING_DATASOURCE_USERNAME: root
        SPRING_DATASOURCE_PASSWORD: root

